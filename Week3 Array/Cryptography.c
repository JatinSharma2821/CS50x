#include<stdio.h>
#include<cs50.h>
#include<string.h>

int main(int argc, string argv[] );

{
	/*
		As we have studied to convert lowercase letter to uppercase .
		In the same way, every message can be encrypted to make it secure


		like - I LOVE YOU --->> J MPWF ZPV 
		nobody can understand its meaning now.

		matter of fact is here every letter is changed by +1;

	I LOVE YOU     +     1      -------------          J MPWF ZPV 
		INPUT          Key         Cipher              Cipher text

	above ----- refers to sort of box that takes input alongwith key and returns ciphertext.
	 This box is called Ciphertext
	 */
}